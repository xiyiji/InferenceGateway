global: { scrape_interval: 1s }
scrape_configs:
  - job_name: vllm
    static_configs: [{ targets: ["host.docker.internal:8000"] }]   # vLLM /metrics via Ray Serve
  - job_name: dcgm
    static_configs: [{ targets: ["dcgm-exporter:9400"] }]
